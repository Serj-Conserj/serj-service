# serj-service

**certbot_m/** — сontains containerization information for Certbot.

**nginx/** — сontains containerization information for Nginx web server.

**compose.yaml** — Docker Compose declarative, specifies the parameters of containers' execution.

**drone.yaml** — DroneCI declarative for serj-service repository. Describes the steps triggered on its modification.
